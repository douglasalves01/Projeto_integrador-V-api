# Fine-tuning only — install: pip install -r requirements.txt -r requirements-llm.txt
trl==0.10.1
bitsandbytes==0.43.3; sys_platform != "darwin"
datasets>=2.18.0
# llama-cpp-python>=0.2.85  # optional GGUF inference backend
